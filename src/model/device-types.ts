
interface Device {
  lbounds: number,
  hbounds: number,
}

interface IDevice extends Device {
  a: number,
  b: number,
}

interface CDevice extends Device {
  a: number,
  b: number,
}

interface GDevice extends Device {
  a: number,
  b: number,
  c: number,
}

interface SDevice extends Device {
  a: number,
  b: number,
  c: number,
}

interface PVDevice extends Device {
  a: number,
  b: number,
  c: number,
}